#include "monty.h"

/**
 * no_work - does nothing
 * @top_stack: top stack of node
 * @line_no: line to opcode
 */
void no_work(stack_t __attribute__((unused)) **top_stack, unsigned int
		 __attribute__((unused)) line_no)
{
	/* Empty function */
}
